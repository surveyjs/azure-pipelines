name: Deploy survey-collaboration demo

trigger: none
pr: none

resources:
  webhooks:
    - webhook: sjsDemosPush
      connection: sjs-demos-incoming-webhook
      filters:
        - path: repository.name
          value: survey-collaboration
        - path: ref
          value: refs/heads/main
          
jobs:
  - job: Deploy
    timeoutInMinutes: 70
    steps:
      - checkout: none
      - task: SSH@0
        displayName: make up-survey-collaboration
        timeoutInMinutes: 60
        inputs:
          failOnStdErr: false
          sshEndpoint: 'sjsdemo'
          runOptions: commands
          commands: make -C /survey/env up-survey-collaboration
